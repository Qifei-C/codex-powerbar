diff --git a/codex-rs/tui_app_server/src/bottom_pane/chat_composer.rs b/codex-rs/tui_app_server/src/bottom_pane/chat_composer.rs
--- a/codex-rs/tui_app_server/src/bottom_pane/chat_composer.rs
+++ b/codex-rs/tui_app_server/src/bottom_pane/chat_composer.rs
@@ -413,6 +413,8 @@ pub(crate) struct ChatComposer {
     status_line_value: Option<Line<'static>>,
     /// Raw string value of the status line, preserving newlines that `Line::from()` strips.
     status_line_raw_value: Option<String>,
+    /// Last known footer content width available to the external status-line command.
+    status_line_available_width: std::cell::Cell<Option<usize>>,
     status_line_enabled: bool,
     // Agent label injected into the footer's contextual row when multi-agent mode is active.
     active_agent_label: Option<String>,
@@ -538,6 +540,7 @@ impl ChatComposer {
             windows_degraded_sandbox_active: false,
             status_line_value: None,
             status_line_raw_value: None,
+            status_line_available_width: std::cell::Cell::new(None),
             status_line_enabled: false,
             active_agent_label: None,
         };
@@ -3813,6 +3816,10 @@ impl ChatComposer {
     pub(crate) fn set_status_line_raw(&mut self, raw: Option<String>) {
         self.status_line_raw_value = raw;
     }
+
+    pub(crate) fn status_line_available_width(&self) -> Option<usize> {
+        self.status_line_available_width.get()
+    }
 
     pub(crate) fn set_status_line_enabled(&mut self, enabled: bool) -> bool {
         if self.status_line_enabled == enabled {
@@ -4269,6 +4276,7 @@ impl ChatComposer {
                 };
                 let available_width =
                     hint_rect.width.saturating_sub(FOOTER_INDENT_COLS as u16) as usize;
+                self.status_line_available_width.set(Some(available_width));
                 let status_line_active = uses_passive_footer_status_layout(&footer_props);
                 let status_line_lines = if status_line_active {
                     // Use the raw value (which preserves newlines) for multiline splitting,
diff --git a/codex-rs/tui_app_server/src/bottom_pane/mod.rs b/codex-rs/tui_app_server/src/bottom_pane/mod.rs
--- a/codex-rs/tui_app_server/src/bottom_pane/mod.rs
+++ b/codex-rs/tui_app_server/src/bottom_pane/mod.rs
@@ -1185,6 +1185,10 @@ impl BottomPane {
         }
     }
 
+    pub(crate) fn status_line_available_width(&self) -> Option<usize> {
+        self.composer.status_line_available_width()
+    }
+
     pub(crate) fn set_status_line_enabled(&mut self, enabled: bool) {
         if self.composer.set_status_line_enabled(enabled) {
             self.request_redraw();
diff --git a/codex-rs/tui_app_server/src/chatwidget/status_surfaces.rs b/codex-rs/tui_app_server/src/chatwidget/status_surfaces.rs
--- a/codex-rs/tui_app_server/src/chatwidget/status_surfaces.rs
+++ b/codex-rs/tui_app_server/src/chatwidget/status_surfaces.rs
@@ -457,6 +457,9 @@ impl ChatWidget {
     /// Collects codex runtime data as env vars for the external status command.
     fn status_line_command_env_vars(&mut self) -> Vec<(String, String)> {
         let mut vars = Vec::new();
+        if let Some(width) = self.bottom_pane.status_line_available_width() {
+            vars.push(("POWERBAR_WIDTH".into(), width.to_string()));
+        }
         if let Some(pct) = self.status_line_context_used_percent() {
             vars.push(("CODEX_CONTEXT_USED_PCT".into(), pct.to_string()));
         }
